import { Typography } from '@mui/material'

const Logo = ({color}) => {
      return (
        <Typography color={color} fontWeight="700" fontSize="1.7rem">
            Apex
        </Typography>
    )
}

export default Logo